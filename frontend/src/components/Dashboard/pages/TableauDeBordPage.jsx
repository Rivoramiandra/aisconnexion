import React from 'react';
import { Typography, Box } from '@mui/material';

const TableauDeBordPage = () => {
    return (
        <Box>
            <Typography variant="h4" gutterBottom>
                Tableau de Bord
            </Typography>
            <Typography>
                Contenu du tableau de bord...
            </Typography>
        </Box>
    );
};

export default TableauDeBordPage; // Export par défaut